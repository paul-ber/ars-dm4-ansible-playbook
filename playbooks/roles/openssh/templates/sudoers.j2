# Configuration sudo pour maverik - droits limités
maverik ALL=(ALL) NOPASSWD: /sbin/reboot, /usr/bin/apt-get update, /usr/bin/apt-get upgrade, /usr/bin/apt-get dist-upgrade

# Configuration sudo pour charlie - tous les droits avec timeout
Defaults:charlie timestamp_timeout=15
charlie ALL=(ALL) ALL
